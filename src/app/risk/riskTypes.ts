export type RiskSource = { tag: string; label?: string; value: number | null; unit?: string; start?: string | null; end: string; filed?: string; accession?: string; url?: string; documentUrl?: string; revised?: boolean };
export type RiskPoint = { fy: number; fp: string; kind: string; end: string; value: number | null; sources: RiskSource[]; formula?: string; calculations?: any[] };
export type RiskMetric = { id: string; label: string; pillar: string; format: string; value: number | null; prior: number | null; delta: number | null; zone: { level: string; label: string }; why: string; note?: string; question: string; formula: string; inputs: { label: string; value: number | null }[]; calculations: any[]; series: RiskPoint[]; sources: RiskSource[]; thresholds: string | null; classification: string; trajectory?: { direction: string; steps: number } };
export type RiskProfile = { basis: string; periods: (RiskPoint & { filed?: string; form?: string; accession?: string })[]; industry: { isBank: boolean; isFinancial: boolean; label: string }; metrics: RiskMetric[]; watchItems: { id: string; label: string; severity: string; pillar: string; reason: string; question: string }[]; coverage: { available: number; total: number; missing: string[] }; stressInputs: Record<string, { value: number | null; formula?: string; sources: RiskSource[] }>; zScore: any; models: { zmijewski: any; beneish: any }; notes: string[] };
export type RiskData = { ticker: string; cik: string; companyName: string; sic: string; sicDescription: string; current: RiskProfile; annual: RiskProfile; generatedAt: string };
